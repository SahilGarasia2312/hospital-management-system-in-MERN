# Production environment — used when running `npm run build`
# This ensures the correct API URL is baked into the React bundle on Render
REACT_APP_API_URL=https://hospital-management-system-in-mern-2.onrender.com/api
